# Souche Express TypeScript API

API robuste, sécurisée, internationalisée, avec authentification JWT et PostgreSQL.

## Quick Start

```bash
cp .env.example .env
npm install
docker-compose up -d
npm run dev